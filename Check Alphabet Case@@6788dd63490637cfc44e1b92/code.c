#include <stdio.h>

int main() {
    char a;
    scanf("%c", &a);
    if (((int)a) >= a && ((int)a) <= z){
        printf("Uppercase");
    }
    else {
        printf("Lowercase");
    }
    return 0;
}